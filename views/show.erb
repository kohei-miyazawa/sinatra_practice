<h2><%= @memo[0]["title"] %></h2>
<div class="memo_body">
  <p><%= @memo[0]["body"] %></p>
</div>

<div class="flex_box">
  <a class="btn" href="/memos/edit/<%= @memo[0]["id"] %>">変更</a>
  <form method="post" action="/memos/<%= @memo[0]["id"] %>">
    <input type="hidden" name="_method" value="delete">
    <input type="submit" class="btn" value="削除">
  </form>
</div>
